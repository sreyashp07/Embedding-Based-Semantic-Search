# Embedding-Based Semantic Search

A semantic search system using FastAPI and Streamlit.
Search documents by meaning, not just keywords.

## Run Backend
cd backend
uvicorn main:app --reload

## Run Frontend
streamlit run frontend/app.py